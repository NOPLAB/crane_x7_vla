data_logger:
  ros__parameters:
    # Output settings
    output_dir: "/workspace/data/logs"

    # Episode settings
    episode_length: 100  # Number of steps per episode
    auto_start_recording: true  # Start recording automatically on launch

    # Data collection rate
    collection_rate: 10.0  # Hz (data collection frequency)

    # Camera settings
    use_camera: true  # Enable RGB camera logging (set to true for real robot with D435)
    use_depth: false  # Enable depth image logging

    # Image settings
    image_width: 640  # Camera image width (0 = use original)
    image_height: 480  # Camera image height (0 = use original)
    image_quality: 90  # JPEG quality (1-100)

    # Joint configuration
    arm_joint_names:
      - "crane_x7_shoulder_fixed_part_pan_joint"
      - "crane_x7_shoulder_revolute_part_tilt_joint"
      - "crane_x7_upper_arm_revolute_part_twist_joint"
      - "crane_x7_upper_arm_revolute_part_rotate_joint"
      - "crane_x7_lower_arm_fixed_part_joint"
      - "crane_x7_lower_arm_revolute_part_joint"
      - "crane_x7_wrist_joint"

    gripper_joint_names:
      - "crane_x7_gripper_finger_a_joint"

    # Topic names
    joint_states_topic: "/joint_states"
    rgb_image_topic: "/camera/color/image_raw"
    depth_image_topic: "/camera/aligned_depth_to_color/image_raw"
    camera_info_topic: "/camera/color/camera_info"

    # Data format
    save_format: "tfrecord"  # npz or tfrecord

    # RLDS (Open X-Embodiment) settings
    dataset_name: "crane_x7"  # Dataset identifier for RLDS format
    language_instruction_topic: "/task/language_instruction"  # Topic for task descriptions
    default_language_instruction: "manipulate the object"  # Default instruction if topic not available

    # Dataset statistics (for OpenVLA normalization)
    compute_dataset_statistics: true  # Compute action statistics across episodes
    statistics_output_path: "/workspace/data/logs/dataset_statistics.json"
