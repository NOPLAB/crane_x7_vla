# SPDX-License-Identifier: MIT
# SPDX-FileCopyrightText: 2025 nop
#
# OpenPI-specific requirements for CRANE-X7
# Requires: requirements-base.txt
#
# Based on Physical Intelligence OpenPI v0.1.0 dependencies

# JAX ecosystem (JAX itself installed in Dockerfile)
flax==0.12.1
jaxtyping==0.3.3
equinox>=0.11.8
orbax-checkpoint==0.11.30

# OpenPI-specific data processing
augmax>=0.3.4
imageio>=2.36.1

# OpenPI-specific utilities
ml_collections==1.1.0
numpydantic>=1.6.6
beartype>=0.18.0,<0.23.0
treescope>=0.1.7
polars>=1.30.0
tyro>=0.9.5
tqdm-loggable>=0.2
fsspec[gcs]>=2024.6.0

# lerobot has complex dependencies - install separately if needed:
# pip install lerobot
