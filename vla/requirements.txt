# CRANE-X7 OpenVLA Fine-tuning Requirements (Ultra-Minimal for Production)
# Size-optimized version: removes all optional dependencies
# Estimated size reduction: 3-5GB from original

# Core OpenVLA dependencies (required for inference)
torch>=2.2.0
torchvision>=0.17.0
transformers==4.57.1
tokenizers==0.22.1
timm==1.0.21
Pillow>=10.0.0

# Fine-tuning specific (LoRA only)
peft==0.17.1
accelerate>=0.27.0
bitsandbytes>=0.43.0

# Data processing (minimal - no TensorFlow if not training from OXE)
# Comment out tensorflow-cpu and tensorflow-datasets if you only need inference
# tensorflow-cpu>=2.15.0
# tensorflow-datasets==4.9.9

# Minimal CV library
opencv-python-headless>=4.8.0
numpy>=1.24.0

# Logging (optional - remove if not needed)
# wandb>=0.16.0
tqdm>=4.66.0

# Note: This minimal configuration is suitable for:
# - Inference-only deployments
# - LoRA fine-tuning without TensorFlow data loaders
# - Environments with strict size constraints
